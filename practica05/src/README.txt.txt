Méndez Ávila Luis Geovanni			nro de cuenta: 317143980

Comentario:

En el problema 6 tuve complicaciones.

Mi idea era hacer que mi algoritmo detectara cuántos números primos
había entre el número dado y 2, para determinar el tamaño del arreglo.
Para eso use un bucle for que fuera de 2 a menor a n y si detectaba un
número primo (usando el operador modulo en una condicion if) se sumara 1
cada vez que el modulo fuera diferente de 0.

Lo que ya no supe hacer fue cómo ir encontrando los números primos y
asignarlos a una posición del arreglo.

Deje como comentario en el programa la firma del problema 6 y la parte que
le correspondía en el metodo main, para que por lo menos pudieran compilar
los demas problemas que ya tenía bien.